"""Run the modelstamp command-line interface with ``python -m modelstamp``."""

from __future__ import annotations

from .cli import main

if __name__ == "__main__":  # pragma: no cover
    raise SystemExit(main())
